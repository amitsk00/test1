function validateData() {
    console.log("inside the JS function");
    // call other functions to check data
    var isValid = true;
    /*
        if (isValid === true) {
            console.log("validation success, PHP will be called")
            // PHP called for backend execution
            $.ajax({
                url: "mytest1.php",
                type: "POST",
                success: function (result) {
                    alert(result)
                }
            })
        }
        else {
            console.log("validation failed, check the errors")
        }
        */
}
